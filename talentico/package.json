{
  "name": "talentico",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "api": "json-server --watch db.json --port 3001",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "json-server": "^0.17.4",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}
